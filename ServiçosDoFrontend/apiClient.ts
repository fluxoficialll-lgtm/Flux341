import { fluxClient } from './fluxClient';

/**
 * apiClient
 * Facade for the fluxClient to provide a unified communication interface
 * for the BFF (Back-end for Front-end) screen services.
 */
export const apiClient = fluxClient;
